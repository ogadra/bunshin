worker_processes auto;
# 非 root (nginx ユーザー) 実行のため、書込み可能な /tmp へ pid を置く。
pid /tmp/nginx.pid;

events {
    worker_connections 1024;
}

http {
    log_format json_combined escape=json
        '{"time":"$time_iso8601",'
        '"remote_addr":"$remote_addr",'
        '"method":"$request_method",'
        '"uri":"$request_uri",'
        '"status":$status,'
        '"body_bytes_sent":$body_bytes_sent,'
        '"request_time":$request_time,'
        '"upstream_response_time":"$upstream_response_time",'
        '"upstream_addr":"$upstream_addr"}';

    access_log /dev/stdout json_combined;
    error_log /dev/stderr warn;

    server_tokens off;

    # access_by_lua から require("runner_url") 等を解決するための検索パス。
    lua_package_path "/usr/local/openresty/nginx/lua/?.lua;;";

    server {
        listen 8080;
        server_name _;

        resolver ${NGINX_RESOLVER} valid=10s;
        client_max_body_size 1m;

        include /etc/nginx/errors.conf;

        location /health {
            access_log off;
            default_type text/plain;
            return 200 'OK';
        }

        # /api/* は broker /resolve でセッションを解決してから runner へ本流 proxy_pass する。
        # auth_request の代わりに access_by_lua + ngx.location.capture を使うことで、
        # 元のメソッド/ボディ/SSE を保持しつつ broker の実ステータス (503 等) を透過する
        # (auth_request は非 2xx を 401/403/500 に潰してしまう)。
        location /api/ {
            set $runner_url "";
            set $resolve_set_cookie "";
            set $session_reassigned "";

            access_by_lua_file /usr/local/openresty/nginx/lua/resolve.lua;

            # broker が返した Set-Cookie (runner_id) と再割当てシグナルをクライアントへ伝える。
            # runner 自身の Set-Cookie (session_id) は proxy 応答でそのまま返る。
            add_header Set-Cookie $resolve_set_cookie always;
            add_header X-Session-Reassigned $session_reassigned always;

            proxy_pass $runner_url$request_uri;
            proxy_hide_header X-Runner-Url;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Port $server_port;

            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }

        # broker /resolve へのサブリクエスト専用。access_by_lua から ngx.location.capture で呼ぶ。
        # internal により外部から直接アクセスできない。ボディは送らない (resolve は cookie のみ参照)。
        location = /_resolve {
            internal;
            set $broker_upstream http://${BROKER_HOST}:8080;
            proxy_pass $broker_upstream/resolve;
            proxy_pass_request_body off;
            proxy_set_header Content-Length "";
            proxy_set_header Cookie $http_cookie;
            proxy_set_header X-Runner-Url "";
            proxy_connect_timeout 5s;
            proxy_read_timeout 10s;
        }
    }
}
