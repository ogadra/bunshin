# Port the runner listens on.
RUNNER_PORT=3000

# Broker URL for runner registration. Required.
BROKER_URL=http://broker:8080

# STACK_NAME identifies the runtime environment for privateHost resolution.
STACK_NAME=local
