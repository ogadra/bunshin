experimental-features = nix-command flakes
flake-registry = /etc/nix/registry.json
keep-derivations = false
keep-outputs = false
# nix-path embeds the pinned store paths, so the build generates it.
include /etc/nix/nix-path.conf
