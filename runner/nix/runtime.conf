experimental-features = nix-command flakes
flake-registry = /etc/nix/registry.json
keep-derivations = false
keep-outputs = false
# nix-path is generated at build time (it embeds the pinned store paths) so it
# cannot be inlined here; include the generated file rather than templating it.
include /etc/nix/nix-path.conf
