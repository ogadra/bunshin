apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: nginx
  namespace: bunshin
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /health
        port: 8080
  targetRef:
    group: ""
    kind: Service
    name: nginx
