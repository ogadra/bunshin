name: Deploy Runner

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled
        run: echo "Deploy Runner workflow is disabled."

# name: Deploy Runner
#
# on:
#   workflow_run:
#     workflows: ["Runner"]
#     types: [completed]
#     branches: [main]
#
# permissions:
#   id-token: write
#   contents: read
#
# concurrency:
#   group: deploy-runner
#   cancel-in-progress: false
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     if: github.event.workflow_run.conclusion == 'success'
#     timeout-minutes: 15
#     steps:
#       - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
#         with:
#           ref: ${{ github.event.workflow_run.head_sha }}
#       - uses: aws-actions/configure-aws-credentials@d979d5b3a71173a29b74b5b88418bfda9437d885 # v6.1.1
#         with:
#           role-to-assume: ${{ secrets.DEPLOY_RUNNER_ROLE_ARN }}
#           aws-region: ap-northeast-1
#       - uses: aws-actions/amazon-ecr-login@fa648b43de3d4d023bcb3f89ed6940096949c419 # v2.1.5
#         id: ecr-login
#       - name: Build and push image
#         env:
#           ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
#           IMAGE_TAG: ${{ github.event.workflow_run.head_sha }}
#         run: |
#           docker build \
#             -t "$ECR_REGISTRY/bunshin/runner:${IMAGE_TAG}" \
#             -t "$ECR_REGISTRY/bunshin/runner:${IMAGE_TAG::7}" \
#             -t "$ECR_REGISTRY/bunshin/runner:latest" \
#             runner/
#           docker push "$ECR_REGISTRY/bunshin/runner:${IMAGE_TAG}"
#           docker push "$ECR_REGISTRY/bunshin/runner:${IMAGE_TAG::7}"
#           docker push "$ECR_REGISTRY/bunshin/runner:latest"
#       - name: Deploy to ECS
#         run: |
#           aws ecs update-service \
#             --cluster bunshin \
#             --service bunshin-runner \
#             --force-new-deployment
#           aws ecs wait services-stable \
#             --cluster bunshin \
#             --services bunshin-runner
